<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:avalonDock="http://schemas.xceed.com/wpf/xaml/avalondock"
                    xmlns:avalonDockControls="clr-namespace:Xceed.Wpf.AvalonDock.Controls;assembly=Xceed.Wpf.AvalonDock">
    <!--<Style TargetType="{x:Type avalonDockControls:LayoutDocumentTabItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutDocumentTabItem}">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="#CCC" 
                            BorderThickness="0,0,1,0" 
                            Padding="8,2">
                        <ContentPresenter Content="{Binding Model.Title, RelativeSource={RelativeSource TemplatedParent}}"  
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFEAF7FF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <!--<Style TargetType="{x:Type avalonDockControls:LayoutDocumentPaneControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutDocumentPaneControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                       
                        <avalonDockControls:DocumentPaneTabPanel 
                        x:Name="HeaderPanel" 
                        Grid.Row="0" 
                        IsItemsHost="True" 
                        Margin="2,0,0,0"
                        Background="Transparent"   
                            Panel.ZIndex="1" />

                       
                            <Border Grid.Row="1" 
                            Background="Red" 
                            BorderBrush="#CCC" 
                            BorderThickness="1,0,1,1" 
                            CornerRadius="0,0,3,3">
                                <ContentPresenter x:Name="PART_SelectedContentHost" 
                                          ContentSource="SelectedContent" />
                            </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->



    <!--<Style TargetType="{x:Type avalonDockControls:LayoutDocumentPaneControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutDocumentPaneControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <avalonDockControls:DocumentPaneTabPanel x:Name="HeaderPanel" 
                                                                  Grid.Row="0" 
                                                                  Panel.ZIndex="1" 
                                                                  IsItemsHost="True" 
                                                                  Margin="2,0,0,0" 
                                                                  Background="Transparent"/>
                          
                        <Border Grid.Row="1" 
                                Background="Transparent" 
                                BorderBrush="#CCC" 
                                BorderThickness="1" 
                                CornerRadius="0,0,3,3">
                            <ContentPresenter x:Name="PART_SelectedContentHost" 
                                               ContentSource="SelectedContent"
                                             
                                              />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    
   
    <Style TargetType="{x:Type avalonDockControls:LayoutAnchorableTabItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutAnchorableTabItem}">
                    <Border x:Name="border" 
                          Background="Transparent" 
                          BorderBrush="#CCC" 
                          BorderThickness="0,0,1,0" 
                          Padding="6,2">
                        <ContentPresenter Content="{Binding Model.Title, RelativeSource={RelativeSource TemplatedParent}}" 
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFEAF7FF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
 
    <Style TargetType="{x:Type avalonDockControls:LayoutAnchorablePaneControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutAnchorablePaneControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                         
                        <Border x:Name="titleBar" 
                                Background="Transparent" 
                                BorderBrush="#3F3F46" 
                                BorderThickness="1,1,1,0" 
                                CornerRadius="3,3,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="title" 
                                           Text="{Binding Model.Title, RelativeSource={RelativeSource TemplatedParent}}" 
                                           Foreground="White" 
                                           Margin="5,2" 
                                           VerticalAlignment="Center"/>
                              
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="2">
                                    <Button x:Name="PART_AutoHide" 
                                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                                            Content="📌" 
                                            ToolTip="自动隐藏" 
                                            Margin="0,0,2,0"/>
                                    <Button x:Name="PART_Hide" 
                                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                                            Content="❌" 
                                            ToolTip="关闭" 
                                            Margin="0,0,2,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <Border Grid.Row="1" 
                                Background="White" 
                                BorderBrush="#3F3F46" 
                                BorderThickness="1,0,1,1" 
                                CornerRadius="0,0,3,3">
                            <ContentPresenter x:Name="PART_SelectedContentHost" 
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <DataTemplate x:Key="TitleTemplate">
        <Border>
            <Label Content="ss"/>
        </Border>
    </DataTemplate>

    <Style TargetType="{x:Type avalonDock:DockingManager}">
        <Setter Property="Background" Value="Transparent"/>
       
       
    </Style>

</ResourceDictionary>