<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:control="clr-namespace:Editor.Control.Text">
    <Style TargetType="{x:Type control:TextEditable}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type control:TextEditable}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <TextBlock  x:Name="PART_TextBlack" Text="{Binding Text,RelativeSource={RelativeSource TemplatedParent}}"/>
                            <TextBox x:Name="PART_TextBox"   Text="{Binding Text,RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" TargetName="PART_TextBlack"/>
                            <Setter Property="Visibility" Value="Visible" TargetName="PART_TextBox"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="False">
                            <Setter Property="Visibility" Value="Visible" TargetName="PART_TextBlack"/>
                            <Setter Property="Visibility" Value="Collapsed" TargetName="PART_TextBox"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>