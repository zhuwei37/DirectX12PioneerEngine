<UserControl x:Class="Editor.View.Preview.SceneNodePreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.View.Preview"
             xmlns:control="clr-namespace:Editor.Control.TreeView"
             mc:Ignorable="d"
             VerticalAlignment="Stretch"
             d:DesignHeight="450" d:DesignWidth="800" >
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       
        <control:PreviewSceneTreeView Grid.Row="1" ItemsSource="{Binding SceneNodes}"  SelectedCommand="{Binding SelectSceneNodeCommand}"  Background="Transparent" >
            <control:PreviewSceneTreeView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="创建空对象" Command="{Binding CreateSceneNodeCommand}"/>
                    <MenuItem Header="创建3D对象">
                        <MenuItem Header="立方体" Command="{Binding CreateBoxGameObjectCommand}"/>
                        <MenuItem Header="球体" Command="{Binding CreateSphereGameObjectCommand}"/>
                        <MenuItem Header="平面" Command="{Binding CreateGridGameObjectCommand}"/>
                    </MenuItem>
                </ContextMenu>
            </control:PreviewSceneTreeView.ContextMenu>
        </control:PreviewSceneTreeView>
       
    </Grid>
</UserControl>
