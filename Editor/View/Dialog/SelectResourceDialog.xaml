<Window x:Class="Editor.View.Dialog.SelectResourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Editor.View.Dialog"
        mc:Ignorable="d"
        Title="选择资源" Height="450" Width="800" WindowStartupLocation="CenterScreen" WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="Transparent">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <Image Source="{Binding Icon}" Grid.Row="0"/>
                                <Border x:Name="PART_Bor"  Grid.Row="1" CornerRadius="5">
                                    <Label HorizontalAlignment="Center" Margin="0,2,0,2" Content="{Binding Title}"  VerticalAlignment="Center"/>
                                    <!--<Label Content="{Binding Title}" x:Name="PART_Lable" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>-->
                                </Border>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="BlueViolet" TargetName="PART_Bor"/>
                            </Trigger>
                            <!--<EventTrigger RoutedEvent="Doub"-->
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        
        <Style TargetType="ListBox">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel ItemHeight="80" ItemWidth="100" Orientation="Horizontal" 
                                   />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border>
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListBox
            ItemsSource="{Binding ResourceList}"
            SelectedValue="{Binding SelectedResource}"
            ></ListBox>
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" Margin="2" HorizontalAlignment="Right">
                <Button Content=" 确定" x:Name="Confirm" Click="Confirm_Click"/>
                <Button Content="取消"  x:Name="Cancel" Click="Cancel_Click" Margin="5,0,10,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
