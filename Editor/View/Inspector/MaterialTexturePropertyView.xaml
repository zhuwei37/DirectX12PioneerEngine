<UserControl x:Class="Editor.View.Inspector.MaterialTexturePropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.View.Inspector"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             xmlns:control="clr-namespace:Editor.Control"
             xmlns:dd="urn:gong-wpf-dragdrop"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style TargetType="Image">
            <Setter Property="Source" Value="/Resource/Image/Icon/Texture.png"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasTexture}" Value="False">
                    <Setter Property="Source" Value="/Resource/Image/Icon/AddTexture.png"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Image 
             AllowDrop="True"
            dd:DragDrop.IsDropTarget="True"
            dd:DragDrop.UseDefaultDragAdorner="True"
            dd:DragDrop.UseDefaultEffectDataTemplate="True"
            dd:DragDrop.DropHandler="{Binding}"
            control:ItemClick.ClickCommand="{Binding SelectTextureCommand}"
            >
        </Image>
        <!--<Popup IsOpen="False" x:Name="popup" >
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="LostFocus">
                    <b:ChangePropertyAction PropertyName="IsOpen"  TargetName="popup" Value="False">

                    </b:ChangePropertyAction>
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <Border>
                <TextBlock Text="好水水水水水水水水水水水水水水水水水水水" Foreground="Red"/>
            </Border>
        </Popup>-->
    </Grid>
</UserControl>
