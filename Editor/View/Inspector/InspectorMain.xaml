<UserControl x:Class="Editor.View.Inspector.InspectorMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.View.Inspector"
             xmlns:vm="clr-namespace:Editor.ViewModel.Inspector"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid  Background="Transparent">
        <ContentControl  Content="{Binding CurrentViewModel}" Background="Transparent">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:NULLInspectorViewModel}">
                    <local:NULLInspector/>
                </DataTemplate>
                <DataTemplate   DataType="{x:Type vm:ActorComponentInspectorViewModel}">
                    <local:ActorComponentInspector/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:MaterialInpectorViewModel}">
                    <local:MaterialInpector/>
                </DataTemplate>
            </ContentControl.Resources>
            <ContentControl.Triggers>
                
            </ContentControl.Triggers>
        </ContentControl>
    </Grid>
</UserControl>
