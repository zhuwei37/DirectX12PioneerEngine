<UserControl x:Class="Editor.View.Inspector.MeshRendererComponentInspector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.View.Inspector"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" MinWidth="50"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <Label Content="材质" VerticalAlignment="Center" Grid.Column="0"></Label>
                                <Border Grid.Column="1" CornerRadius="5" BorderBrush="Black" Margin="0,5,0,5" BorderThickness="0.5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Image Source="/Resource/Image/Icon/Material.png" Margin="5,1,5,1" Height="12" Width="12"/>
                                        <Label Content="{Binding Title}" HorizontalAlignment="Center"  VerticalAlignment="Center" Grid.Column="1"/>
                                        <Button HorizontalAlignment="Right" Margin="0,0,10,0" Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl},Path=DataContext.SelectMaterialCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem},Converter={StaticResource ItemToIndex}}" Style="{StaticResource EllipseButtonStyle}" Height="20" Width="20" Grid.Column="2"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Content="Material" Visibility="Collapsed"/>
        <ListBox  Grid.Row="1" Style="{StaticResource StackPanelListBoxStyle}" ItemsSource="{Binding Materials}">
            
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
            <Button Content="添加材质" Command="{Binding AddMaterialCommand}" Margin="0,0,5,0"/>
            <Button Content="删除材质" Command="{Binding RemoveMaterialCommand}" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
